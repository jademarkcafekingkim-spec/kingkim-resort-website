jQuery(function($){$('#kk-gallery-select').on('click',function(){const picker=wp.media({title:'Room gallery',multiple:true,library:{type:'image'}});picker.on('open',function(){const selection=picker.state().get('selection');$('#kk-gallery').val().split(',').filter(Boolean).forEach(id=>selection.add(wp.media.attachment(id)));});picker.on('select',function(){$('#kk-gallery').val(picker.state().get('selection').map(item=>item.id).join(',')).trigger('change');});picker.open();});});
